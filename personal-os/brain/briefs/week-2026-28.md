# Week 2026-28 — Librarian

The vault is empty (fewer than 20 notes — `notes/` and `writing/` don't exist yet). Drop or symlink ~20 markdown notes into `notes/` and the next weekly run will produce a real synthesis.
